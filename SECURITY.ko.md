# 보안 정책

[English](SECURITY.md)

## 지원 버전

보안 수정은 [PyPI](https://pypi.org/project/chemvas/)에 공개된 최신 마이너
릴리스에만 배포합니다. 이전 릴리스에는 수정을 백포트하지 않으므로, 수정을 받으려면
최신 릴리스로 업그레이드해야 합니다.

| 버전 | 지원 여부 |
| --- | --- |
| 0.21.x | 지원 |
| < 0.21 | 미지원 |

## 취약점 제보

보안 취약점은 공개 GitHub 이슈, 토론, 풀 리퀘스트로 제보하지 마십시오.

GitHub의 비공개 제보 기능을 사용해 주십시오:
[Report a vulnerability](https://github.com/dhsohn/Chemvas/security/advisories/new)
(**Security** 탭 → **Report a vulnerability**).

가능한 한 다음 정보를 포함해 주십시오.

- Chemvas 버전(`chemvas --version` 또는 **About Chemvas** 대화상자), Python 버전,
  운영체제, RDKit 추가 기능 설치 여부
- 영향을 받는 진입점: 데스크톱 앱, 특정 CLI 명령, Python API 중 무엇인지
- 문제를 일으키는 최소 입력 파일(`.chemvas`, Chemvas SVG, composition/patch/plan
  JSON, 이미지)과 정확한 재현 절차 또는 명령
- 관찰한 영향과 기대한 동작

## 처리 절차

Chemvas는 한 사람이 관리하므로 아래 기한은 보장이 아니라 목표입니다.

- 제보를 받으면 7일 이내에 확인 답변을 드리는 것을 목표로 합니다.
- 30일 이내에 취약점으로 인정하는지 여부와 수정 계획을 알려 드리겠습니다.
- 인정한 취약점은 새 릴리스에서 수정합니다. 수정은 GitHub 보안 권고(security
  advisory) 및 `CHANGELOG.md` 항목과 함께 공개하며, 제보자가 원하지 않는 경우를
  제외하고 제보자를 기재합니다.

수정 릴리스나 보안 권고가 공개되기 전까지는 제보 내용을 비공개로 유지해 주십시오.

## 범위

Chemvas는 로컬 데스크톱 앱이자 헤드리스 CLI이며 네트워크 서비스를 실행하지
않습니다. 주요 보안 경계는 **신뢰할 수 없는 입력 파일**입니다. 다른 사람에게서 받은
문서, SVG, 매니페스트, 이미지를 열고, 검사하고, 렌더링하고, 처리해도 안전해야 합니다.

범위에 포함되는 것:

- 조작된 파일을 열거나 처리할 때 발생하는 코드 실행, 또는 요청한 입력·출력 외
  파일의 읽기·쓰기
- 문서화된 자원 한도(문서, 이미지, CLI 입력의 바이트·픽셀 한도)를 우회해 발생하는
  충돌, 무응답, 메모리 고갈
- [AGENT_CLI.ko.md](docs/AGENT_CLI.ko.md)의 계약과 달리 입력 파일을 수정하거나
  요청한 `--output` 경로 밖에 쓰는 CLI 명령
- 자동 저장 또는 세션 복구 파일을 통해 같은 컴퓨터의 다른 사용자에게 문서 내용이
  노출되는 문제

범위에 포함되지 않는 것:

- Chemvas가 악화시키지 않는 Qt/PyQt6, RDKit, Pillow 등 의존성의 취약점. 해당
  업스트림 프로젝트에 제보해 주십시오.
- 공격자가 이미 같은 사용자 권한으로 코드를 실행할 수 있어야 성립하는 공격
- 문서화된 한도 안에 있는 큰 입력의 처리 속도 저하
- 과학적 정확성 문제(잘못된 물성, 기하 구조, 레이아웃). 일반
  [이슈](https://github.com/dhsohn/Chemvas/issues)로 제보해 주십시오.
